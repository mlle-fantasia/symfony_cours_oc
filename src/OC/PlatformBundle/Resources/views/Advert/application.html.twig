<ul class="nav nav-pills nav-stacked">
    {% for application in listApplications %}
        <li>
            <a href="{{ path('oc_platform_view', {'id': application.getId()}) }}">
                {{ application.getAuthor() }}
            </a>
        </li>
    {% endfor %}
</ul>